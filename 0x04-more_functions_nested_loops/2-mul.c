#include "main.h"
/**
 * mul - is the multiplication
 * @a: the first int
 * @b: the second int
 * Return: like usual but this time return will be to multiplicator
 */
int mul(int a, int b)
{

	return (a * b);
}
