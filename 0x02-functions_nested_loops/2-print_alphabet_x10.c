#include "main.h"
/**
 * main - is the main function
 * Description: we will inclyse new functions
 * Return: 0 is success
 */
void print_alphabet(void);
{
	int n;
	for (n == 'a'; n <= 'z'; n++)
	{
		_pyrchar(n);
	}
	_putchar('\n');
	return (0);
}
